{% trans_default_domain 'EasyAdminBundle' %}

{% if value == true %}
    <span class="badge badge-danger">{{ 'label.true'|trans }}</span>
{% else %}
    <span class="badge badge-success">{{ 'label.false'|trans }}</span>
{% endif %}